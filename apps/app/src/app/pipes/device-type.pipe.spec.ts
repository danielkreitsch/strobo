import {DeviceTypePipe} from './device-type.pipe';

describe('DeviceTypePipePipe', () => {
  it('create an instance', () => {
    const pipe = new DeviceTypePipe();
    expect(pipe).toBeTruthy();
  });
});
